pipeline {
    agent {
        docker {image 'pythondocker_app'}
    }

    stages {
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        
    }
}
