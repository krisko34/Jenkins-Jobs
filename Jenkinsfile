 stages {
        stage("Add to container") {
            steps {
                cat Dockerfile
            }
        }

        stage("Run script") {
            steps {
                cat requirements.txt
            }
        }
 }
